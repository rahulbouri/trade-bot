# AgentQuant — Streamlit Community Cloud requirements
# Pinned to avoid dependency conflicts.
# google-genai replaces google-generativeai; both langchain-google-genai>=2.0 and
# google-genai>=1.0 share the same underlying google-ai-generativelanguage>=0.7.

# Core data
pandas>=2.0,<3.0
numpy>=1.26,<2.0
pyyaml>=6.0
python-dotenv>=1.0
pydantic>=2.0,<3.0
yfinance>=0.2.28
scipy>=1.12
pyarrow>=16.0
tabulate>=0.9
statsmodels>=0.14
requests>=2.31
pytz>=2024.1
matplotlib>=3.8

# Dashboard
streamlit>=1.39
plotly>=5.17

# Scheduler
apscheduler>=3.10

# LLM — new unified SDK (no conflict)
google-genai>=1.0
langchain>=0.2
langchain-google-genai>=2.0
langchain-core>=0.1
langgraph>=0.0.69
openai>=1.0

# Regime detection
hmmlearn>=0.3
scikit-learn>=1.5
